# DeskRPG 0.1.4

Released: 2026-04-01

## Highlights

- Fixed Windows CLI startup for `npx deskrpg start`.
- Changed the internal `tsx` loader path to use a `file://` URL so Node can resolve `--import` correctly on Windows.
- Keeps the `0.1.3` CLI fixes intact, including SQLite-safe `init` and runtime cleanup commands.

## Verification

- `node --check bin/deskrpg.js`
- `npm run build`
- Docker image build and push for `dandacompany/deskrpg:0.1.4`

## Notes

- npm publishing for `0.1.4` is handled manually.
